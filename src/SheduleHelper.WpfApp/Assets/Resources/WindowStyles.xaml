<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
                    xmlns:controls="clr-namespace:SheduleHelper.WpfApp.Controls">

    <!-- Window Button Base Style -->
    <Style x:Key="WindowButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="42"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Foreground" Value="{DynamicResource DefaultControlForegroundDefaultBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="root" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="root" Property="Background" Value="{DynamicResource DefaultControlBackgroundHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="root" Property="Background" Value="{DynamicResource DefaultControlBackgroundPressedBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Close Button Style -->
    <Style x:Key="WindowCloseButtonStyle" BasedOn="{StaticResource WindowButtonStyle}" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="root" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="root" Property="Background" Value="#E81123"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="root" Property="Background" Value="#C50F1F"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Default CustomWindow Style -->
    <Style x:Key="DefaultCustomWindowStyle" TargetType="{x:Type controls:CustomWindow}">
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="ResizeMode" Value="CanResize"/>
        <Setter Property="Background" Value="{DynamicResource LayerBackgroundFloatingBrush}"/>
        <Setter Property="MinWidth" Value="600"/>
        <Setter Property="MinHeight" Value="320"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome
                    CaptionHeight="32"
                    CornerRadius="6"
                    ResizeBorderThickness="8"
                    GlassFrameThickness="0"
                    UseAeroCaptionButtons="False"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:CustomWindow}">
                    <Border x:Name="PART_RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Title Bar -->
                            <Border Grid.Row="0"
                                    Height="32"
                                    Background="Transparent"
                                    x:Name="PART_TitleBar">
                                <DockPanel VerticalAlignment="Center" LastChildFill="True">
                                    <!-- Title Text -->
                                    <TextBlock Text="{TemplateBinding Title}"
                                               VerticalAlignment="Center"
                                               Margin="12,0"
                                               Foreground="{DynamicResource DefaultControlForegroundDefaultBrush}"
                                               shell:WindowChrome.IsHitTestVisibleInChrome="True"/>

                                    <!-- Window Control Buttons -->
                                    <StackPanel Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Right"
                                                Background="Transparent"
                                                shell:WindowChrome.IsHitTestVisibleInChrome="True">

                                        <!-- Minimize Button -->
                                        <Button x:Name="PART_MinimizeButton"
                                                Style="{StaticResource WindowButtonStyle}">
                                            <TextBlock Text="&#xE921;" FontFamily="Segoe Fluent Icons" FontSize="12"/>
                                        </Button>

                                        <!-- Maximize/Restore Button -->
                                        <Button x:Name="PART_MaximizeRestoreButton"
                                                Style="{StaticResource WindowButtonStyle}">
                                            <TextBlock x:Name="MaximizeRestoreIcon" Text="&#xE922;" FontFamily="Segoe Fluent Icons" FontSize="12"/>
                                        </Button>

                                        <!-- Close Button -->
                                        <Button x:Name="PART_CloseButton"
                                                Style="{StaticResource WindowCloseButtonStyle}">
                                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe Fluent Icons" FontSize="12"/>
                                        </Button>
                                    </StackPanel>
                                </DockPanel>
                            </Border>

                            <!-- Main Content Area -->
                            <ContentPresenter Grid.Row="1" Margin="0"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"/>
                        </Grid>
                    </Border>

                    <!-- Triggers for window state changes -->
                    <ControlTemplate.Triggers>
                        <!-- When window is maximized -->
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="PART_RootBorder" Property="Padding" Value="8"/>
                            <Setter TargetName="PART_RootBorder" Property="CornerRadius" Value="0"/>
                            <Setter TargetName="MaximizeRestoreIcon" Property="Text" Value="&#xE923;"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Apply style by default to all CustomWindows -->
    <Style TargetType="{x:Type controls:CustomWindow}" BasedOn="{StaticResource DefaultCustomWindowStyle}"/>

</ResourceDictionary>