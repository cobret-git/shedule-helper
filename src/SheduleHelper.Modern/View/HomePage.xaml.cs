using Microsoft.UI.Xaml.Controls;

namespace SheduleHelper.Modern.View
{
    /// <summary>
    /// Home page (The Daily Control Center). DataContext is assigned by <see cref="Services.NavigationService"/>.
    /// </summary>
    public sealed partial class HomePage : Page
    {
        public HomePage()
        {
            InitializeComponent();
        }
    }
}
