<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="SheduleHelper.Modern.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SheduleHelper.Modern"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="SheduleHelper.Modern"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--<BreadcrumbBar
            x:Name="AppBreadcrumbBar"
            Grid.Row="0"
            Margin="16,12"
            ItemsSource="{x:Bind NavigationService.Breadcrumbs}"
            ItemClicked="AppBreadcrumbBar_ItemClicked" />-->

        <Frame x:Name="ContentFrame" Grid.Row="1" />
    </Grid>
</Window>
